package ThinkInJava;

/**
 * 内部类
 */
public class InnerClass {
    /**
     * 链接到外部类
     * 1：内部类的对象可以访问外围对象的的所有成员
     * 2：内部类还能访问外围类的所有元素
     * 3：内部类的对象只能在与外部类的对象相关联的情况下才能被创建
     */

    /**
     * 使用.this与.new
     * 1：可以使用类名后面加圆点和this，生成对外部类对象的引用，class.this
     *
     */
}
